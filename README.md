# eshop-project-springboot

***Run userService:***
- `cd userService`
- `mvn clean install`
- `./mvnw spring-boot:run`

***Run priceService:***
- `cd priceService`
- `mvn clean install`
- `./mvnw spring-boot:run`