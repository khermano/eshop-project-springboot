# eshop-project-springboot

***Run userService:***
- `cd userService`
- `mvn clean install`
- `mvn spring-boot:run`

***Run categoryService:***
- `cd categoryService`
- `mvn clean install`
- `mvn spring-boot:run`

***Run productService:***
- `cd productService`
- `mvn clean install`
- `mvn spring-boot:run`
